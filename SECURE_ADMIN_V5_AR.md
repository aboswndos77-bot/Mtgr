# المتجر اليمني — Secure V5

## ما تم تأمينه في هذه المرحلة
- تجميد/إلغاء تجميد الحسابات يتم حصراً عبر Cloud Function بصلاحية ADMIN.
- تعديل إعدادات المنصة يتم حصراً عبر Cloud Function بصلاحية ADMIN.
- انتقالات حالات الطلب للتاجر أصبحت خادمية ومتحققة من ملكية الطلب وتسلسل الحالة.
- رفع إثبات التوصيل أصبح خادمياً مع إشعار للمستهلك.
- معالجة النزاعات تستخدم `disputeId` وتحدّث سجل النزاع إلى RESOLVED عند التسوية.
- تم منع تحديث مستندات المستخدمين مباشرة من العميل لتجنب تغيير `role` أو `isFrozen` أو الحقول الحساسة.
- إنشاء محفظة المنصة يتم تلقائياً عند الحاجة أثناء المعاملات الخادمية.

## ملاحظة تشغيلية
قبل الإنتاج يجب:
1. إضافة `google-services.json` إلى مجلد `app/`.
2. إنشاء مشروع Firebase وربط Android package name.
3. تفعيل Authentication/Email، Firestore، Functions.
4. نشر `firebase deploy --only functions,firestore:rules` من بيئة Node/Firebase CLI.
5. اختبار الشحن والتسوية والنزاعات بحسابات اختبار منفصلة قبل استخدام أموال حقيقية.

هذه النسخة لا تتضمن تكاملاً آلياً مع API رسمي لجيب؛ تعتمد على رقم العملية وإثبات التحويل ومراجعة الإدارة ما لم يتم توفير API رسمي وشروطه.
